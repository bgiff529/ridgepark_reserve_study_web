services:
  - type: web
    name: reserve-study-web
    runtime: docker
    plan: starter
    region: oregon
    dockerfilePath: ./web/Dockerfile
    dockerContext: .
    autoDeploy: true
    healthCheckPath: /
    envVars:
      - key: DEFAULT_VARIANT
        value: 2026_joint_buget_maint
      - key: APP_PASSWORD
        sync: false
